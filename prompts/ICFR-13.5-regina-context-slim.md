# ICFR-13.5 Regina_ClaudeContext.md 축소 리팩터

## 목적
Regina_ClaudeContext.md를 **정적 부트스트랩 전용 문서로 슬림화**한다.
동적 진행 상태·ClaudeICFR.md와 중복되는 부분을 걷어내고, 참조 링크로 대체한다.
stale 발생 지점 자체를 제거하는 것이 목표.

## 방침
이 문서는 claude.ai 새 채팅 시작 시 붙여넣는 개인 온보딩 컨텍스트다.
**정적 정보(프로필·협업 패턴·템플릿·치트시트)만 남기고**, 동적 상태는 ClaudeICFR.md(SSOT)를 참조하게 한다.

## 스코프 가드 (엄수)
- **Regina_ClaudeContext.md 한 파일만** 수정. 다른 문서·코드 절대 건드리지 않는다.
- ClaudeICFR.md는 **읽기만**(참조 문구 정확히 쓰기 위한 섹션 번호 확인용). 수정 금지.
- 없는 사실 지어내지 않는다. 정정은 **확인된 사실만**.
- bypass/무승인 모드 금지 — per-command 승인.

## 작업 상세

### 1. 걷어낼 섹션 (삭제 → 참조로 대체)
- **§4 현재 프로젝트 진행 상태** → 삭제하고 "진행 상태는 ClaudeICFR.md §12 참조" 한 줄로 대체
- **§6 다음 할 일** → 삭제하고 "다음 할 일은 ClaudeICFR.md §13 참조" 한 줄로 대체
  - (ClaudeICFR.md 실제 섹션 번호를 열어서 확인 후 정확히 기입 — §12/13이 맞는지 검증)
- **§2 프로젝트 개요** 중 스택·역할분담·모듈구성 등 ClaudeICFR.md와 중복되는 축약 → 삭제하고 "프로젝트 개요·스택·모듈은 ClaudeICFR.md 참조"로 축약. 단, 레포/경로/SSOT 위치처럼 **부트스트랩에 즉시 필요한 최소 정보**는 남긴다(판단해서 처리, 근거 보고).
- **§5 주요 합의·결정·교훈** 중 ADR-0017/0018 요지 등 ClaudeICFR.md §10에 원본 있는 항목 → 삭제하고 "ADR 목록은 ClaudeICFR.md §10 참조". Docker 캐시 교훈 등 ClaudeICFR.md에 없는 개인 메모성 교훈은 유지 여부 판단 후 보고.

### 2. 유지할 섹션 (이 문서의 존재 이유 — 손대지 않음)
- §1 나는 누구인가 (프로필) — **단, stale 정정만**: "Claude Code Sonnet 4.6" 등 확인된 버전 오류를 현재값으로 수정하거나, 버전 하드코딩 자체를 제거(재발 방지 권장 — 판단 후 보고)
- §3 협업 패턴
- §7 새 채팅 시작 시 첫 메시지 템플릿
- §8 자주 쓰는 명령어 패턴
- §9 협업자 전달 메모

### 3. 갱신 규칙 명시 추가
- 문서 상단 또는 하단에 **한 줄** 추가: "동적 진행 상태는 이 문서에 기재하지 않음 — ClaudeICFR.md가 SSOT" (같은 stale 재발 방지)
- 최하단 갱신일자를 오늘(2026-08-20)로 갱신

## 산출물
1. 슬림화 전/후 섹션 구성 비교(무엇을 지우고 무엇을 참조로 바꿨는지)
2. 판단이 필요했던 지점(§2 최소정보 남김, §5 개인 교훈 유지 여부, §1 버전 처리)의 결정과 근거
3. diff 요약
4. `docs:` 커밋 메시지 초안

## 커밋·push
- `docs:` 커밋 (feat/fix와 분리)
- 코드 무영향·회귀 위험 없는 문서 단일 파일 → ADR-0024 Tier 1(문서)이므로 커밋 후 push 진행
- 단, push 전 diff 요약 보고

## PowerShell 5.1 제약
- `&&` 금지 → `;` 또는 명령 분리
- `grep` 금지 → `Select-String`
- `cd` 금지 → `Set-Location E:\claudeprojects\ICFR`
- bash 경로·heredoc·`2>&1` 금지
- 단일 명령 965 bytes 이내

---
ICFR-13.5-regina-context-slim.md 진행해줘
