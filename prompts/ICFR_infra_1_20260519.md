# ICFR_infra_1_20260519.md — Phase 0 작업1: 인프라 셋업

## 메타 정보

| 항목 | 값 |
|---|---|
| 작업 일시 | 2026-05-19 |
| 작업 유형 | 인프라 셋업 (Docker Compose + 환경 변수 + CI 골조) |
| 담당 | TrustBuilder |
| Phase | Phase 0 — 작업 단위 1 |
| 결정 출처 | claude.ai 사전 승인 |
| 예상 작업 시간 | 30~45분 |
| 영향 파일 | 신규 생성 (인프라 파일 6~7개) |
| 커밋 메시지 제안 | `feat(infra): Docker Compose + 환경변수 + CI 골조 (Phase 0 작업1)` |

---

## 작업 지시

`ClaudeICFR.md` 섹션 3 (기술 스택), 섹션 13 (다음 작업), `CLAUDE.md`를 먼저 확인하고, 아래 인프라 파일들을 순서대로 생성해줘.

이번 작업은 **Phase 0의 첫 번째 작업 단위**다. 백엔드·프론트엔드 코드는 아직 만들지 않고, **개발·운영 환경 골조만** 세팅한다.

---

## 1. `docker-compose.yml` 생성 (레포 루트)

3개 서비스 정의. 모두 single network에서 통신.

### 1.1 postgres 서비스 (PostgreSQL 16)

요구 사항:
- 이미지: `postgres:16-alpine` (경량)
- 컨테이너 이름: `icfr-postgres`
- 포트: 5432:5432
- 환경 변수: `.env`에서 읽음
  - POSTGRES_USER
  - POSTGRES_PASSWORD
  - POSTGRES_DB
- 볼륨 마운트: `postgres_data:/var/lib/postgresql/data` (데이터 영속화)
- 헬스체크: `pg_isready -U $POSTGRES_USER -d $POSTGRES_DB` 5초마다, 5번 재시도
- 재시작 정책: `unless-stopped`
- 네트워크: `icfr-network`

### 1.2 minio 서비스 (S3 호환 파일 저장소)

요구 사항:
- 이미지: `minio/minio:latest`
- 컨테이너 이름: `icfr-minio`
- 포트:
  - 9000:9000 (API)
  - 9001:9001 (콘솔 UI)
- 환경 변수: `.env`에서 읽음
  - MINIO_ROOT_USER
  - MINIO_ROOT_PASSWORD
- 볼륨 마운트: `minio_data:/data`
- 명령어: `server /data --console-address ":9001"`
- 헬스체크: `curl -f http://localhost:9000/minio/health/live` 30초마다
- 재시작 정책: `unless-stopped`
- 네트워크: `icfr-network`

### 1.3 backend 서비스 (FastAPI — placeholder)

이번 작업에서는 아직 코드가 없으므로 **주석 처리된 골조**로 둠. 작업 단위 2에서 실제로 활성화 예정.

요구 사항:
- 다음 형태로 docker-compose.yml에 주석 처리된 섹션 추가:
  ```yaml
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: icfr-backend
  #   ports:
  #     - "8000:8000"
  #   env_file:
  #     - .env
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy
  #   networks:
  #     - icfr-network
  #   # 작업 단위 2에서 활성화 예정
  ```

### 1.4 볼륨·네트워크 정의

```yaml
volumes:
  postgres_data:
  minio_data:

networks:
  icfr-network:
    driver: bridge
```

---

## 2. `.env.example` 생성 (레포 루트)

환경 변수 템플릿. **GitHub에 커밋됨** (안전한 placeholder 값).

```env
# === Database (PostgreSQL) ===
POSTGRES_USER=icfr_user
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=icfr_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg://icfr_user:changeme_in_production@postgres:5432/icfr_db

# === MinIO (S3 호환 파일 저장소) ===
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_in_production
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_ENDPOINT=http://localhost:9000
MINIO_BUCKET=icfr-evidence
MINIO_USE_SSL=false

# === JWT 인증 ===
# 운영 시 반드시 안전한 랜덤 문자열로 교체 (32바이트 이상 권장)
JWT_SECRET_KEY=please_generate_a_strong_random_key_for_production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRES_DAYS=7

# === Application ===
ENVIRONMENT=development
LOG_LEVEL=INFO
APP_NAME=ICFR System
APP_VERSION=0.1.0

# === CORS (Phase 1 추가 예정) ===
# CORS_ALLOWED_ORIGINS=http://localhost:5173
```

---

## 3. `.env` 생성 (로컬 개발용, gitignore됨)

`.env.example`을 복사해서 `.env`로 만들되, 비밀번호 등은 개발용 실제 값으로 채움.

> ⚠️ 이 파일은 절대 commit되면 안 됨. `.gitignore`에 이미 등록되어 있는지 확인 후 보강.

`.env` 내용은 `.env.example`과 동일하되 다음을 변경:
- `JWT_SECRET_KEY` — 32바이트 랜덤 문자열로 생성 (Python `secrets.token_urlsafe(32)` 사용 가능)
- 나머지는 `.env.example` 그대로 유지 (개발 환경)

생성 후 사용자에게 안내:
- ".env 파일이 생성되었습니다. 운영 환경에서는 비밀번호와 JWT_SECRET_KEY를 안전한 값으로 변경해주세요."

---

## 4. `.gitignore` 보강

기존 `.gitignore`를 읽고, 다음 항목이 있는지 확인 후 없으면 추가:

```gitignore
# === Environment ===
.env
.env.local
.env.*.local
!.env.example

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
*.cover

# === Virtual Environment ===
venv/
.venv/
env/

# === Node / Frontend ===
node_modules/
.next/
dist/
.vite/

# === Docker ===
*.log

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === OS ===
.DS_Store
Thumbs.db
desktop.ini
```

이미 있는 항목은 중복 추가하지 말 것.

---

## 5. `.github/workflows/ci.yml` 생성 (CI 골조)

GitHub Actions CI. 골조만 만들고, 작업 단위 2 이후 backend 코드가 들어오면 단계적으로 활성화.

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      # Backend 의존성 설치 (작업 단위 2 이후 활성화)
      # - name: Install backend dependencies
      #   working-directory: ./backend
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -r requirements.txt
      
      # Ruff 린트 (작업 단위 2 이후 활성화)
      # - name: Lint with ruff
      #   working-directory: ./backend
      #   run: ruff check .
      
      # pytest (작업 단위 2 이후 활성화)
      # - name: Run pytest
      #   working-directory: ./backend
      #   run: pytest
      
      - name: CI placeholder
        run: echo "CI pipeline ready. Backend tests will be enabled in Phase 0 작업2."
```

---

## 6. `dev.ps1` 생성 (PowerShell 편의 명령, 레포 루트)

Windows 환경용 편의 스크립트. Make와 같은 역할.

```powershell
# dev.ps1 — ICFR 개발 환경 편의 명령
# 사용법: .\dev.ps1 <command>

param(
    [Parameter(Position=0)]
    [string]$Command = "help"
)

function Show-Help {
    Write-Host ""
    Write-Host "ICFR 개발 환경 편의 명령" -ForegroundColor Cyan
    Write-Host "================================" -ForegroundColor Cyan
    Write-Host ""
    Write-Host "사용법: .\dev.ps1 <command>"
    Write-Host ""
    Write-Host "명령:" -ForegroundColor Yellow
    Write-Host "  up        모든 컨테이너 시작 (백그라운드)"
    Write-Host "  down      모든 컨테이너 종료"
    Write-Host "  restart   모든 컨테이너 재시작"
    Write-Host "  logs      모든 컨테이너 로그 (실시간)"
    Write-Host "  ps        컨테이너 상태 확인"
    Write-Host "  clean     컨테이너 + 볼륨 삭제 (데이터 손실 주의)"
    Write-Host "  shell-db  PostgreSQL 콘솔 진입"
    Write-Host "  minio     MinIO 콘솔 URL 출력 (http://localhost:9001)"
    Write-Host "  help      이 도움말"
    Write-Host ""
}

switch ($Command) {
    "up" {
        Write-Host "컨테이너 시작 중..." -ForegroundColor Green
        docker compose up -d
    }
    "down" {
        Write-Host "컨테이너 종료 중..." -ForegroundColor Yellow
        docker compose down
    }
    "restart" {
        Write-Host "컨테이너 재시작 중..." -ForegroundColor Yellow
        docker compose restart
    }
    "logs" {
        docker compose logs -f
    }
    "ps" {
        docker compose ps
    }
    "clean" {
        Write-Host "컨테이너와 볼륨을 모두 삭제합니다. 데이터가 사라집니다." -ForegroundColor Red
        $confirm = Read-Host "정말 진행할까요? (yes/no)"
        if ($confirm -eq "yes") {
            docker compose down -v
            Write-Host "삭제 완료" -ForegroundColor Green
        } else {
            Write-Host "취소됨" -ForegroundColor Yellow
        }
    }
    "shell-db" {
        docker compose exec postgres psql -U icfr_user -d icfr_db
    }
    "minio" {
        Write-Host "MinIO 콘솔: http://localhost:9001" -ForegroundColor Cyan
        Write-Host "사용자: minioadmin (또는 .env의 MINIO_ROOT_USER)"
        Write-Host "비밀번호: .env의 MINIO_ROOT_PASSWORD 참조"
    }
    default {
        Show-Help
    }
}
```

---

## 7. `infra/README.md` 생성

협업자(Regina)와 미래 합류자를 위한 안내.

```markdown
# 인프라 사용 안내

ICFR 시스템의 개발 환경 셋업 가이드.

## 필수 도구

| 도구 | 용도 | 설치 링크 |
|---|---|---|
| Docker Desktop | 컨테이너 실행 | https://www.docker.com/products/docker-desktop |
| Git | 코드 동기화 | https://git-scm.com/ |
| Python 3.12+ | 백엔드 개발 (TrustBuilder) | https://www.python.org/ |
| Node.js 20+ | 프론트엔드 개발 (Regina) | https://nodejs.org/ |

Windows 10/11에서는 Docker Desktop 사용 시 **WSL2 활성화**가 필요할 수 있음.

## 첫 셋업 (한 번만)

```powershell
# 1. 레포 클론 (이미 했다면 생략)
git clone https://github.com/jeremydev99/claude-icfr.git
cd claude-icfr

# 2. 환경 변수 파일 복사
copy .env.example .env

# 3. .env 파일 열어서 비밀번호·시크릿 변경 (운영 시)
# 개발 환경에서는 기본값 그대로 OK

# 4. 컨테이너 시작
.\dev.ps1 up

# 5. 컨테이너 상태 확인
.\dev.ps1 ps
```

## 일상 명령

| 작업 | 명령 |
|---|---|
| 컨테이너 시작 | `.\dev.ps1 up` |
| 컨테이너 종료 | `.\dev.ps1 down` |
| 로그 보기 | `.\dev.ps1 logs` |
| 상태 확인 | `.\dev.ps1 ps` |
| DB 콘솔 | `.\dev.ps1 shell-db` |
| MinIO 콘솔 정보 | `.\dev.ps1 minio` |

## 서비스 접속

- **FastAPI**: http://localhost:8000 (작업 단위 2 이후)
- **PostgreSQL**: localhost:5432 (외부 DB 도구로 접속)
- **MinIO API**: http://localhost:9000
- **MinIO 콘솔**: http://localhost:9001

## 트러블슈팅

### Q: 포트 충돌 (5432, 9000 등이 이미 사용 중)
A: `.env`에서 포트 변경 또는 기존 서비스 종료. `docker-compose.yml`의 ports도 함께 변경.

### Q: Docker Desktop이 시작되지 않음
A: Windows 기능에서 "WSL2" 활성화. 재부팅 필요.

### Q: 컨테이너 로그에 권한 오류
A: `.\dev.ps1 clean` 후 `.\dev.ps1 up` 다시 실행. 볼륨 권한 초기화됨.

### Q: PostgreSQL 데이터를 완전히 리셋하고 싶음
A: `.\dev.ps1 clean` 실행. 모든 컨테이너·볼륨 삭제 후 다시 시작.

## 백업·복구 (Phase 2 이후 추가 예정)

운영 환경 진입 시 추가 예정.
```

---

## 진행 방식

### Step 1: 사전 확인
1. 현재 `.gitignore` 내용 확인
2. `docker --version` 으로 Docker 설치 여부 확인 (없으면 사용자에게 안내)

### Step 2: 파일 생성
위 7개 파일을 순서대로 생성:
1. `docker-compose.yml`
2. `.env.example`
3. `.env` (gitignore 보호 확인 후)
4. `.gitignore` 보강
5. `.github/workflows/ci.yml`
6. `dev.ps1`
7. `infra/README.md`

### Step 3: 검증
- `docker compose config` 명령으로 docker-compose.yml 문법 검증
- 파일 생성 결과를 표로 정리해서 사용자에게 보고

### Step 4: 커밋 메시지 제안 + 사용자 OK 대기
변경 요약 표 + 커밋 메시지 제안 후 사용자 OK 받으면 `git add . → git commit → git push` 진행.

커밋 메시지 제안: `feat(infra): Docker Compose + 환경변수 + CI 골조 (Phase 0 작업1)`

### Step 5: 일일 진행 로그 추가
작업 완료 후 ClaudeICFR.md 섹션 18에 다음 한 줄 자동 추가:
```
- **TrustBuilder**: Phase 0 작업1 완료 — Docker Compose + 환경변수 + CI 골조 셋업. 다음: 백엔드 골조 (작업2).
```
이것은 자동 푸시 대상 (`chore(log): ... [auto]`).

---

## 완료 후 알릴 것 (사용자에게 안내)

1. ✅ Phase 0 작업1 완료
2. 다음 작업: ICFR_backend_1_YYYYMMDD.md (FastAPI 프로젝트 + DB 연결 + Alembic + JWT 골조)
3. 사용자가 직접 테스트 해볼 수 있는 명령:
   - `.\dev.ps1 up` — 컨테이너 시작
   - `.\dev.ps1 ps` — 상태 확인 (postgres, minio 두 개가 healthy 상태여야 함)
   - http://localhost:9001 → MinIO 콘솔 (로그인 가능해야 함)
   - `.\dev.ps1 down` — 종료
4. Regina에게 git pull 안내
